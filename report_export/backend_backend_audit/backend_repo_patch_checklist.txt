BACKEND REPO PATCH CHECKLIST

Apply these updates to the backend repository that powers:
https://rork-easy-seas-vp1-2nep.onrender.com/

Required file-by-file updates:

1. Update backend DB connection logic
Target file:
- backend/db.js or equivalent deployed DB connection file

Required behavior:
- Try both DB endpoint candidates:
  a) raw endpoint
  b) endpoint + /rpc
- Treat these failures as database unavailable instead of unhandled crashes:
  - {"group":"api","code":"not_found","message":"The requested resource was not found"}
  - Failed to fetch
  - fetch failed
  - VersionRetrievalFailure
  - Failed to retrieve remote version
- Keep logs showing which endpoint candidate was attempted.

2. Update tRPC route handling for app compatibility
Target file:
- app-side client already updated locally in expo/lib/trpc.ts
- backend should correctly serve the expected tRPC endpoint path

Required behavior:
- Ensure the backend deployment accepts the route shape used by the app.
- Confirm whether production serves:
  - /trpc
  - /api/trpc
- If only one is active, either support both or keep deployment and client fully aligned.

3. Update data router payload support
Target file:
- backend/trpc/routes/data.js

Required additions:
- cruises
- crewRecognitionEntries
- crewRecognitionSailings

Required behavior:
- Save these fields in the user profile record.
- Return these fields in getAllUserData.
- Preserve existing stored values when omitted from partial saves.

4. Restore calendar feed route
Target file:
- backend/hono.js or equivalent Hono entry

Required behavior:
- Add GET /calendar-feed/:token
- Read calendar_feeds by token
- Return text/calendar with the stored ICS payload
- Return 404 when token is not found

5. Update app router registrations
Target file:
- backend/trpc/app-router.js

Current stale registrations observed:
- example
- data
- calendar
- royalCaribbeanSync
- cruiseDeals

Still missing from stale snapshot:
- crewRecognition
- dailyLuck

Required behavior:
- Register every router currently used by the app.
- Redeploy after router registration changes.

Final validation after redeploy:
- Root health endpoint returns success.
- /trpc responds for current procedures.
- Cloud sync no longer returns not_found.
- Calendar feed route responds.
- crewRecognition procedures resolve.
- dailyLuck procedures resolve.
- SurrealDB connection succeeds with the production env configuration.
