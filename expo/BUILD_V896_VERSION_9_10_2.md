# Easy Seas v896 — Version Metadata Fix

This package is rebuilt from v895 with the app metadata incremented for the next build.

## Version changes

- `expo.version`: `9.10.2`
- `ios.buildNumber`: `9.10.2`
- `android.versionCode`: `9102`

## Functional code changes

No functional Sync Now, completed cruise sync, storage, or logo/header logic was changed in this package.

Going forward, every generated build should increment `app.json` version metadata. The prior latest app version was treated as `9.10.1`, so this build is `9.10.2`.
