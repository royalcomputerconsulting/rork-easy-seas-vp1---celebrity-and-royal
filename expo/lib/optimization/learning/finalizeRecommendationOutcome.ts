import{stableModelFingerprint}from'../models/statistics';import type{RecommendationOutcomeInput,RecommendationOutcomeRecord}from'./types';
function continuation(action:string){return['PLAY_ONE_MORE_SESSION','CONTINUE_UNTIL_TARGET','PROFIT_PROTECTED_PUSH','EXCELLENT_OPPORTUNITY'].includes(action)}
export function finalizeRecommendationOutcome(input:RecommendationOutcomeInput):RecommendationOutcomeRecord{const r=input.recommendation;if(r.ownerProfileId.trim()==='')throw new Error('Recommendation owner profile is required.');const warnings:string[]=[];const targetReached=r.recommendedTargetPoints===null?null:input.actualFinalPoints>=r.recommendedTargetPoints;const targetPredictionCorrect=targetReached===null?null:(r.probabilityOfSuccess??0.5)>=0.5?targetReached:!targetReached;const expectedContinue=continuation(r.action);const stopContinueCorrect=input.playContinuedAfterRecommendation===null?null:expectedContinue===input.playContinuedAfterRecommendation;const actualLoss=input.actualAdditionalLoss===null?Math.max(0,-input.actualCasinoResult):Math.max(0,input.actualAdditionalLoss);const actualIncrementalNetValue=input.actualCertificateValueRealized===null?null:input.actualCertificateValueRealized-actualLoss;if(input.actualAdditionalLoss===null)warnings.push('Actual additional loss was unavailable and was conservatively inferred from final casino result.');if(input.recommendationFollowed===null)warnings.push('Whether the recommendation was followed is unknown.');return{id:`recommendation-outcome:${stableModelFingerprint([r.id,input.cruiseOutcomeId,input.finalizedAt])}`,ownerProfileId:r.ownerProfileId,recommendationId:r.id,cruiseOutcomeId:input.cruiseOutcomeId,cruiseSailDate:input.cruiseSailDate,action:r.action,recommendedTargetPoints:r.recommendedTargetPoints,predictedSuccessProbability:r.probabilityOfSuccess,predictedExpectedAdditionalLoss:r.expectedAdditionalLoss,predictedRiskAdjustedIncrementalEv:r.riskAdjustedIncrementalExpectedValue,actualFinalPoints:input.actualFinalPoints,targetReached,actualCasinoResult:input.actualCasinoResult,actualAdditionalCoinIn:input.actualAdditionalCoinIn,actualAdditionalLoss:actualLoss,actualCertificateCode:input.actualCertificateCode,actualCertificateValueRealized:input.actualCertificateValueRealized,actualIncrementalNetValue,recommendationFollowed:input.recommendationFollowed,playContinuedAfterRecommendation:input.playContinuedAfterRecommendation,stopContinueCorrect,targetPredictionCorrect,confidence:r.confidence,modelVersion:r.modelVersion,engineVersion:r.engineVersion,finalizedAt:input.finalizedAt,warnings};}
