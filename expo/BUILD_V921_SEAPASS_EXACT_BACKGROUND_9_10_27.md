# Easy Seas v921 / 9.10.27 SeaPass Exact Background Repair

## Fixed
- Port field erase mask now uses the same field background constant used by the other SeaPass editable field masks: `#F4F4F5`.
- Removed the darker neutral/off-white tone introduced in v920.
- Kept the no-source-sampling approach for the port field so the baked-in old port text cannot be copied into the legal paragraph.

## Not changed
- Sync Now adaptive live-only engine remains from v920.
- Completed-cruise sync unchanged.
- Logo assets unchanged.
