const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),Module=require('node:module'),ts=require('typescript');const root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8'),filename=path.join(root,'lib/travelReadinessVault.ts'),out=ts.transpileModule(read('lib/travelReadinessVault.ts'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022},fileName:filename}).outputText;const mod=new Module(filename,module);mod.filename=filename;mod.paths=Module._nodeModulePaths(path.dirname(filename));mod._compile(out,filename);const lib=mod.exports,record=lib.createTravelReadinessRecord('c','2026-08-21');record.confirmation='SECRET123';record.invoiceNotes='$999 SECRET';record.passportVisa='P123456';record.emergencyContacts='555-1111';record.flights='PHX to MIA';record.checks.flights=true;record.documents=[{id:'d',kind:'boarding',name:'boarding.pdf',uri:'x',addedAt:'x'}];assert.equal(lib.travelReadinessPercent(record),14);const shared=lib.buildRedactedTravelReadinessShare({shipName:'Icon',sailDate:'2026-09-01',returnDate:'2026-09-08',record});assert.match(shared,/PHX to MIA/);assert.doesNotMatch(shared,/SECRET123|\$999 SECRET|P123456|555-1111/);assert.match(shared,/intentionally omitted/);const screen=read('app/travel-readiness-vault.tsx');assert.match(screen,/travel-vault-share-redacted/);assert.match(screen,/expo-document-picker/);assert.match(read('app/today-on-cruise.tsx'),/today-travel-readiness-vault/);console.log('PASS build410_travel_readiness_vault_regression');
