const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),Module=require('node:module'),ts=require('typescript');const root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8'),filename=path.join(root,'lib/backToBackOperationalPlan.ts'),out=ts.transpileModule(read('lib/backToBackOperationalPlan.ts'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022},fileName:filename}).outputText;const mod=new Module(filename,module);mod.filename=filename;mod.paths=Module._nodeModulePaths(path.dirname(filename));mod._compile(out,filename);const lib=mod.exports;const same=lib.buildBackToBackOperationalPlan([{shipName:'Icon',sailDate:'2026-09-01',returnDate:'2026-09-08',departurePort:'Miami',nights:7},{shipName:'Icon',sailDate:'2026-09-08',returnDate:'2026-09-15',departurePort:'Miami',nights:7}]);assert.equal(same.transitions[0].risk,'low');assert.equal(same.transitions[0].terminalTransferRequired,false);const sameShipGap=lib.buildBackToBackOperationalPlan([{shipName:'Icon',sailDate:'2026-09-01',returnDate:'2026-09-08',departurePort:'Miami',nights:7},{shipName:'Icon',sailDate:'2026-09-09',returnDate:'2026-09-16',departurePort:'Miami',nights:7}]);assert.equal(sameShipGap.transitions[0].risk,'medium');assert.equal(sameShipGap.hotelNights,1);const change=lib.buildBackToBackOperationalPlan([{shipName:'Icon',sailDate:'2026-09-01',returnDate:'2026-09-08',departurePort:'Miami',nights:7},{shipName:'Beyond',sailDate:'2026-09-09',returnDate:'2026-09-16',departurePort:'Fort Lauderdale',nights:7}]);assert.equal(change.transitions[0].risk,'high');assert.equal(change.hotelNights,1);assert.ok(change.checklist.some(x=>x.category==='visa'&&x.required));assert.ok(change.checklist.some(x=>x.category==='laundry'&&x.required));assert.match(read('app/back-to-back-trip-builder.tsx'),/Terminal transfer minutes/);assert.match(read('app\/(tabs)\/scheduling.tsx'),/b2b-open-operational-builder/);console.log('PASS build407_b2b_operational_trip_builder_regression');
