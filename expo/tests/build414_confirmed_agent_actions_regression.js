const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),Module=require('node:module'),ts=require('typescript');const root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8'),filename=path.join(root,'lib/agentConfirmedActions.ts'),out=ts.transpileModule(read('lib/agentConfirmedActions.ts'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022},fileName:filename}).outputText,old=Module._load;Module._load=(r,p,m)=>r.startsWith('@/')?{}:old(r,p,m);let lib;try{const mod=new Module(filename,module);mod.filename=filename;mod.paths=Module._nodeModulePaths(path.dirname(filename));mod._compile(out,filename);lib=mod.exports}finally{Module._load=old}const offer={id:'o1',offerCode:'26ABC',title:'Casino offer'},certificate={id:'c1',certificateCode:'2607A02A',label:'Certificate',status:'available'},cruise={id:'b1',shipName:'Icon of the Seas',sailDate:'2026-09-26'};assert.equal(lib.parseAgentConfirmedAction({message:'Shortlist offer 26ABC',offers:[offer],certificates:[],cruises:[]}).kind,'shortlist-offer');assert.equal(lib.parseAgentConfirmedAction({message:'Mark certificate 2607A02A used',offers:[],certificates:[certificate],cruises:[]}).kind,'mark-certificate-used');const reminder=lib.parseAgentConfirmedAction({message:'Remind me on 2026-09-01 to check in',offers:[],certificates:[],cruises:[]});assert.equal(lib.buildAgentReminderEvent(reminder).startDate,'2026-09-01');assert.equal(lib.parseAgentConfirmedAction({message:'Start casino session',offers:[],certificates:[],cruises:[]}).kind,'start-session');assert.equal(lib.parseAgentConfirmedAction({message:'Prepare host brief',offers:[],certificates:[],cruises:[]}).kind,'prepare-host-brief');assert.equal(lib.parseAgentConfirmedAction({message:'Open Icon of the Seas cruise',offers:[],certificates:[],cruises:[cruise]}).kind,'open-cruise');const provider=read('state/AgentXProvider.tsx');assert.match(provider,/Confirmation gate/);assert.match(provider,/confirmAgentAction/);assert.match(provider,/persistConversation/);assert.match(read('app/ask-my-data.tsx'),/ask-my-data-saved-conversations/);console.log('PASS build414_confirmed_agent_actions_regression');
