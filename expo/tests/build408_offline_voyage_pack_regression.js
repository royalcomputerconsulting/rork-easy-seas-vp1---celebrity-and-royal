const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),Module=require('node:module'),ts=require('typescript');const root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8'),filename=path.join(root,'lib/offlineVoyagePack.ts'),out=ts.transpileModule(read('lib/offlineVoyagePack.ts'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022},fileName:filename}).outputText,old=Module._load;Module._load=(r,p,m)=>r.startsWith('@/')?{}:old(r,p,m);let lib;try{const mod=new Module(filename,module);mod.filename=filename;mod.paths=Module._nodeModulePaths(path.dirname(filename));mod._compile(out,filename);lib=mod.exports}finally{Module._load=old}const cruise={id:'c',shipName:'Icon',sailDate:'2026-09-01',returnDate:'2026-09-02',reservationNumber:'R',casinoHost:'Alex',casinoHostEmail:'a@b.com',itinerary:[{day:1,date:'2026-09-01',port:'Miami',isSeaDay:false,latitude:1,longitude:2},{day:2,date:'2026-09-02',port:'Sea',isSeaDay:true}]};const forecasts=['2026-09-01','2026-09-02'].map(dateKey=>({cruiseId:'c',dateKey,isStale:false}));const full=lib.buildOfflineVoyagePackStatus({cruise,forecasts,certificateCount:2,certificateDocumentCount:2,deckMappingCount:3,calendarEvents:[{id:'e',title:'Cruise',startDate:'2026-09-01',endDate:'2026-09-02',type:'cruise',cruiseId:'c'}]});assert.equal(full.completionPercent,100);assert.equal(full.missingDays.length,0);const partial=lib.buildOfflineVoyagePackStatus({cruise:{...cruise,itinerary:[]},forecasts:[],certificateCount:0,certificateDocumentCount:0,deckMappingCount:0,calendarEvents:[]});assert.ok(partial.completionPercent<100);assert.deepEqual(partial.missingDays,['2026-09-01','2026-09-02']);assert.match(read('app/offline-voyage-pack.tsx'),/offline-voyage-pack-preload/);assert.match(read('app/today-on-cruise.tsx'),/today-offline-voyage-pack/);console.log('PASS build408_offline_voyage_pack_regression');
